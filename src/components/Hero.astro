---
import siteSettings from "../content/settings/site.json";

const { hero } = siteSettings;
---

<section
  class="relative min-h-screen flex items-center justify-center overflow-hidden"
>
  <div
    id="hero-bg"
    class="absolute inset-0 z-0"
    style={hero.image
      ? `background-image: url('${hero.image}'); background-repeat: no-repeat; background-size: cover; opacity: ${(hero.backgroundOpacity || 100) / 100};`
      : ""}
  >
    <!-- Subtle white gradient overlay from bottom to top -->
    <div
      class="absolute inset-0 z-10 pointer-events-none bg-gradient-to-t from-white/90 to-transparent"
    >
    </div>
  </div>

  <div class="container mx-auto px-4 py-20 relative z-20">
    <div class="flex justify-between md:gap-10 sm:px-6">
      <div class="flex flex-col justify-center md:items-start">
        <div class="md:text-left md:max-w-3xl">
          <h2 class="text-5xl md:text-6xl font-medium bg-clip-text text-tertiary-600 mb-6">
            {hero.subtitle}
          </h2>
          <p class="text-lg md:text-xl text-black mb-12 max-w-2xl">
            {hero.description}
          </p>
        </div>
        <div class="flex flex-row gap-4">
          <a
            href="/projetos"
            class="px-8 py-4 bg-tertiary-600 text-white rounded-lg hover:bg-tertiary-700 transition-colors font-semibold shadow-lg"
          >
            Ver Projetos
          </a>
          <a
            href="#contato"
            class="px-8 py-4 bg-white text-tertiary-600 rounded-lg hover:bg-gray-50 transition-colors font-semibold shadow-lg border-2 border-tertiary-600"
          >
            Entre em Contato
          </a>
        </div>
      </div>
    </div>
  </div>

  <div
    class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce"
  >
    <svg
      class="w-6 h-6 text-primary-600"
      fill="none"
      stroke="currentColor"
      viewBox="0 0 24 24"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="2"
        d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
    </svg>
  </div>

  <script>
    const heroBg = document.getElementById("hero-bg");

    if (heroBg) {
      const handleScroll = () => {
        const scrolled = window.scrollY;
        // Subtle parallax: move background vertically at 30% of scroll speed
        const yPos = scrolled * 0.3;
        heroBg.style.backgroundPosition = `center ${yPos}px`;
      };

      window.addEventListener("scroll", handleScroll, { passive: true });
    }
  </script>
</section>
